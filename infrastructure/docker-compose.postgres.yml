version: '3.8'

services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: default
      POSTGRES_PASSWORD: M3oVGBtuDs2X
      POSTGRES_DB: verceldb
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
